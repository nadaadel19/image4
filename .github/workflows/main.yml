on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: "dockerhub login"
        run: docker login --username nadaadel19 --password dckr_pat_yaSEBZTju8RZRlTXRNHDisMCs3g

      


